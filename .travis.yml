language: ruby

sudo: required

services:
    - docker
    
before_install:
  - pip install codecov

before_install:
  - pip install --user codecov

install:
    - ./kingdom.sh start test
    - sleep 60
    - ./kingdom.sh log

script:
    - ./kingdom.sh test

after_success:
  - codecov
